interface:
  display_name: "Render Static Sites"
  short_description: "Deploy and configure static sites on Render."
  default_prompt: "Use $render-static-sites to deploy this static site on Render."
