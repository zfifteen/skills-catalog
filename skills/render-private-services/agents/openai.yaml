interface:
  display_name: "Render Private Services"
  short_description: "Configure internal-only private services on Render."
  default_prompt: "Use $render-private-services to configure a Render private service for this app."
