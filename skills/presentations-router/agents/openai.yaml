interface:
  display_name: "Presentation"
  short_description: "Route slide requests to the preinstalled capability"
  default_prompt: "Use $presentation to create or edit a presentation artifact."
policy:
  allow_implicit_invocation: false
