interface:
  display_name: "Sales Pipeline"
  short_description: "Create spreadsheets with the Sales Pipeline template"
  icon_large: "./assets/preview.png"
  default_prompt: "Create a new spreadsheet with this template."
policy:
  allow_implicit_invocation: false
