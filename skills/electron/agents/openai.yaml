interface:
  display_name: Zoom Meeting SDK (Electron)
  short_description: Zoom Meeting SDK for Electron desktop applications. Use when
    embedding Zoom meetings in an Electron app with the Node addon wrapper, JWT auth,
    join/start flows, settings controllers, and raw data integration.
policy:
  allow_implicit_invocation: false
