interface:
  display_name: Zoom Meeting SDK (Unreal Engine)
  short_description: Zoom Meeting SDK for Unreal Engine wrapper integrations. Use
    when building Unreal projects that embed Zoom meetings with C++ and Blueprint
    wrappers, including wrapper-to-SDK mapping concerns.
policy:
  allow_implicit_invocation: false
