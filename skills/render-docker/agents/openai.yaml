interface:
  display_name: "Render Docker"
  short_description: "Build and deploy Docker containers on Render."
  default_prompt: "Use $render-docker to prepare this Docker app for deployment on Render."
