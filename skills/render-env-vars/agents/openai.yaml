interface:
  display_name: "Render Env Vars"
  short_description: "Configure Render environment variables, secrets, and env groups."
  default_prompt: "Use $render-env-vars to configure environment variables for this Render app."
