interface:
  display_name: "Emulator QA"
  short_description: "Drive Android emulator QA with adb"

dependencies:
  tools:
    - type: "cli"
      value: "adb"
      description: "Android Debug Bridge for emulator/app control"
    - type: "cli"
      value: "python3"
      description: "Run UI tree helper scripts"
    - type: "cli"
      value: "gradle"
      description: "Build and install app variants"
