interface:
  display_name: "Render Web Services"
  short_description: "Configure Render web services and health checks."
  default_prompt: "Use $render-web-services to configure this app as a Render web service."
