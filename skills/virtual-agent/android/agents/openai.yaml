interface:
  display_name: Zoom Virtual Agent - Android
  short_description: Zoom Virtual Agent Android integration via WebView. Use for Java/Kotlin
    bridge callbacks, native URL handling, support_handoff relay, and lifecycle-safe
    embedding.
policy:
  allow_implicit_invocation: false
