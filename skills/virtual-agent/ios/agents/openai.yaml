interface:
  display_name: Zoom Virtual Agent - iOS
  short_description: Zoom Virtual Agent iOS integration via WKWebView. Use for Swift/Objective-C
    script injection, message handlers, support_handoff relay, and URL routing policies.
policy:
  allow_implicit_invocation: false
