interface:
  display_name: "Render Domains"
  short_description: "Configure custom domains and TLS on Render."
  default_prompt: "Use $render-domains to configure a custom domain for this Render service."
