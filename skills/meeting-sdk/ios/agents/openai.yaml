interface:
  display_name: Zoom Meeting SDK (iOS)
  short_description: Zoom Meeting SDK for iOS native apps. Use when embedding Zoom
    meetings in iOS with default/custom UI, PKCE + SDK auth, host start with ZAK,
    and mobile lifecycle handling.
policy:
  allow_implicit_invocation: false
