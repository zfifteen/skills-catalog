interface:
  display_name: Zoom Meeting SDK (Android)
  short_description: Zoom Meeting SDK for Android native apps. Use when embedding
    Zoom meetings in Android with default/custom UI, PKCE + SDK auth, join/start flows,
    and Meeting SDK API integration.
policy:
  allow_implicit_invocation: false
