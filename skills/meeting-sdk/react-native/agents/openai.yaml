interface:
  display_name: Zoom Meeting SDK (React Native)
  short_description: Zoom Meeting SDK for React Native. Use when embedding Zoom meetings
    in React Native iOS/Android apps with @zoom/meetingsdk-react-native, JWT auth,
    join/start flows, platform setup, and native bridge troubleshooting.
policy:
  allow_implicit_invocation: false
